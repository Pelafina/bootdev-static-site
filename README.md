# bootdev-static-site

A simple python based static site generator made following the curriculum of the boot.dev "Build a Static Site Generator in Python" Course.
Takes standard markdown and converts it into a website.
If you want to use this, simply create your markdown, place it in /static and run main.sh, by default it will run a server on localhost:8888.

Note: im pretty sure this only works on Linux and Mac, I use Arch btw.
